name: Search API Pipeline

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

jobs:
  scrape-and-upload:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install SearXNG and Dependencies
        run: |
          git clone https://github.com/searxng/searxng.git --depth 1
          cd searxng
          pip install .
          pip install internetarchive

      - name: Create SearXNG Settings
        run: |
          mkdir -p searxng-config
          cat > searxng-config/settings.yml << 'SETTINGS'
          use_default_settings: true
          search:
            formats:
              - html
              - json
          server:
            secret_key: "scrapebin-github-actions-key"
            bind_address: "127.0.0.1:8080"
          SETTINGS

      - name: Start SearXNG
        run: |
          export SEARXNG_SETTINGS_PATH=$(pwd)/searxng-config/settings.yml
          cd searxng
          nohup python -m searx.webapp > /tmp/searxng.log 2>&1 &
          echo $! > /tmp/searxng.pid
          echo "Waiting for SearXNG to start..."
          for i in $(seq 1 60); do
            if curl -s http://127.0.0.1:8080 > /dev/null 2>&1; then
              echo "SearXNG is ready!"
              break
            fi
            echo "Waiting... ($i/60)"
            sleep 2
          done

      - name: Run Scraper, Update, and Upload
        shell: python
        env:
          IA_ACCESS_KEY: ${{ secrets.IA_ACCESS_KEY }}
          IA_SECRET_KEY: ${{ secrets.IA_SECRET_KEY }}
        run: |
          import os, json, time, requests, re, sys
          from datetime import datetime, timezone
          from internetarchive import get_session

          ia_access = os.environ.get("IA_ACCESS_KEY")
          ia_secret = os.environ.get("IA_SECRET_KEY")
          if not ia_access or not ia_secret:
              print("ERROR: IA_ACCESS_KEY or IA_SECRET_KEY is missing!")
              sys.exit(1)

          SEARXNG_URL = "http://127.0.0.1:8080/search"
          QUERIES_FILE = "queries.txt"
          RESULTS_FILE = "results.jsonl"
          METADATA_FILE = "metadata.json"
          README_FILE = "README.md"

          if not os.path.exists(QUERIES_FILE):
              with open(QUERIES_FILE, "w") as f:
                  f.write("open source\npython\n")
          if not os.path.exists(README_FILE):
              with open(README_FILE, "w") as f:
                  f.write("# Search API\n\n")
          if not os.path.exists(RESULTS_FILE):
              open(RESULTS_FILE, "w").close()

          with open(QUERIES_FILE, "r") as f:
              queries = [line.strip() for line in f if line.strip()]

          total_new_results = 0
          with open(RESULTS_FILE, "a", encoding="utf-8") as outfile:
              for query in queries:
                  try:
                      print("Searching: " + query)
                      res = requests.get(
                          SEARXNG_URL,
                          params={"q": query, "format": "json"},
                          timeout=30
                      )
                      if res.status_code == 200:
                          data = res.json()
                          results = data.get("results", [])
                          for r in results:
                              item = {
                                  "query": query,
                                  "date": datetime.now(timezone.utc).isoformat(),
                                  "search_provider": r.get("engine", "unknown"),
                                  "title": r.get("title", ""),
                                  "url": r.get("url", ""),
                                  "snippet": r.get("content", "")
                              }
                              outfile.write(json.dumps(item, ensure_ascii=False) + "\n")
                              total_new_results += 1
                          print("Got " + str(len(results)) + " results for: " + query)
                      else:
                          print("Bad status " + str(res.status_code) + " for: " + query)
                      time.sleep(2)
                  except Exception as e:
                      print("Error for " + query + ": " + str(e))
                      continue

          print("Total new results this run: " + str(total_new_results))

          whole_size = os.path.getsize(RESULTS_FILE)
          with open(RESULTS_FILE, "r", encoding="utf-8") as f:
              total_historical_results = sum(1 for _ in f)

          metadata = {
              "last_updated": datetime.now(timezone.utc).isoformat(),
              "number_of_results": total_historical_results,
              "whole_size_bytes": whole_size
          }
          with open(METADATA_FILE, "w", encoding="utf-8") as f:
              json.dump(metadata, f, indent=2)

          def format_size(size_bytes):
              for unit in ["B", "KB", "MB", "GB"]:
                  if size_bytes < 1024.0:
                      return str(round(size_bytes, 2)) + " " + unit
                  size_bytes = size_bytes / 1024.0
              return str(round(size_bytes, 2)) + " TB"

          with open(README_FILE, "r", encoding="utf-8") as f:
              text = f.read()

          updates = [
              ("Number of Results:", str(total_historical_results)),
              ("Time when updated:", metadata["last_updated"]),
              ("Whole size:", format_size(whole_size))
          ]
          for label, val in updates:
              pattern = "^" + re.escape(label) + ".*$"
              replacement = label + " " + val
              if re.search(pattern, text, flags=re.MULTILINE):
                  text = re.sub(pattern, replacement, text, flags=re.MULTILINE)
              else:
                  text = text.rstrip() + "\n" + replacement

          with open(README_FILE, "w", encoding="utf-8") as f:
              f.write(text + "\n")

          print("Authenticating with Internet Archive...")
          config = {
              "s3": {
                  "access": ia_access,
                  "secret": ia_secret
              }
          }
          session = get_session(config)
          item = session.get_item("ScrapeBin-Search")
          print("Uploading files...")
          item.upload(
              ["results.jsonl", "metadata.json"],
              metadata={"title": "ScrapeBin Search API", "mediatype": "data"}
          )
          print("Upload Successful!")

      - name: Commit and Push README changes
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add README.md
          git diff --quiet && git diff --staged --quiet || (git commit -m "Update README stats automatically" && git push)

      - name: Cleanup
        run: |
          kill $(cat /tmp/searxng.pid) 2>/dev/null || true
          rm -rf results.jsonl metadata.json searxng/ searxng-config/
